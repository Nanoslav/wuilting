import React from 'react';

const Spinner = () => {
    return (
        <div>
            <div className="loading loading-spinner loading-lg loader-wrapper">
                loading...
            </div>
        </div>
    );
};

export default Spinner;