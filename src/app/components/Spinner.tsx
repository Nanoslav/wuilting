import React from 'react';

const Spinner = () => {
    return (
        <div>
            <div className="loader-wrapper">
                loading...
            </div>
        </div>
    );
};

export default Spinner;